{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos mínimos de la ventana principal de Latido. Todo lo que no sea imprescindible para leer fuentes y avisar al usuario se queda fuera.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "notification:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://**" }, { "url": "http://**" }]
    }
  ]
}
